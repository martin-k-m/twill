# Build output
/twill
/twill.exe
/dist/

# Go
*.test
*.out
coverage.txt

# Model artifacts written by examples (examples/save_load.tw)
*.bin

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
Thumbs.db

# Local Go temp / cache (avoids a Windows Application Control block on system temp)
/.gotmp/
/.gocache/
rust/
*.vsix

# Benchmark artefacts. The scripts under bench/ regenerate them, and the numbers
# that matter are quoted in docs/BENCHMARKS.md, so the raw output is not tracked.
bench/results/
