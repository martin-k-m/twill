[package]
name = "raster-tensor"
version = "0.1.0"
edition = "2021"
description = "Raster's differentiable tensor engine, ported from internal/tensor"
license = "MIT"

# This list stays empty. It is the machine-checkable form of design principle 4,
# and `cargo tree` printing one line is the CI check for it.
[dependencies]

[profile.release]
opt-level = 3
codegen-units = 1
