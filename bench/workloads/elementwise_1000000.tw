# Elementwise chain at n=1000000: tanh(exp(x/4) * x) reduced to a scalar. Three
# passes over the buffer plus a reduction, so the cost is memory traffic and
# transcendental throughput rather than any one kernel.
seed(1)
let x = randn(1000000)
fn work() = mean(tanh(exp(x / 4.0) * x))
work
